import React from 'react';
import { ScrollView, Text } from 'react-native';

const UserProfilComments = () => {
	return (
		<ScrollView>
			<Text>UserProfilComments</Text>
		</ScrollView>
	);
};

export default UserProfilComments;
