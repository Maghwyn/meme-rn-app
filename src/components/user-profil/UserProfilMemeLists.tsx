import React from 'react';
import { ScrollView, Text } from 'react-native';

const UserProfilMemeLists = () => {
	return (
		<ScrollView>
			<Text>Memes</Text>
		</ScrollView>
	);
};

export default UserProfilMemeLists;
