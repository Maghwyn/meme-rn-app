import React from 'react';
import { ScrollView, Text } from 'react-native';

const UserProfilLikedMemeLists = () => {
	return (
		<ScrollView>
			<Text>Memes Liked</Text>
		</ScrollView>
	);
};

export default UserProfilLikedMemeLists;
